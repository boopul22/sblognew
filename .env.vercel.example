# ========================================
# 🚀 VERCEL DEPLOYMENT ENVIRONMENT VARIABLES
# ========================================
# Copy these to your Vercel project dashboard under Settings → Environment Variables

# ========================================
# 🗄️ SUPABASE CONFIGURATION (Required)
# ========================================
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ========================================
# 📁 STORAGE PROVIDER SELECTION
# ========================================
# Choose your storage provider (optional, defaults to 'supabase')
VITE_STORAGE_PROVIDER=supabase
# OR use Cloudflare R2:
# VITE_STORAGE_PROVIDER=cloudflare-r2

# ========================================
# ☁️ CLOUDFLARE R2 CONFIGURATION (Optional)
# ========================================
# Only required if using VITE_STORAGE_PROVIDER=cloudflare-r2

# Frontend Variables (Safe for client-side)
VITE_CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
VITE_CLOUDFLARE_R2_BUCKET_NAME=blog-images
VITE_CLOUDFLARE_R2_PUBLIC_URL=https://pub-YOUR_ACCOUNT_ID.r2.dev/blog-images

# Serverless Function Variables (Server-side only - KEEP PRIVATE!)
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
CLOUDFLARE_R2_ACCESS_KEY_ID=your_r2_access_key_id
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
CLOUDFLARE_R2_BUCKET_NAME=blog-images
CLOUDFLARE_R2_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com

# ========================================
# 🌍 PRODUCTION ENVIRONMENT
# ========================================
NODE_ENV=production

# ========================================
# 📝 INSTRUCTIONS FOR VERCEL DEPLOYMENT
# ========================================
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings → Environment Variables
# 3. Add each variable above with your actual values
# 4. Set the environment to: Production, Preview, and Development
# 5. Deploy your project

# ========================================
# 🔒 SECURITY NOTES
# ========================================
# ✅ VITE_* variables are safe for client-side (included in build)
# ❌ Never expose CLOUDFLARE_R2_SECRET_ACCESS_KEY in client code
# ❌ Never expose SUPABASE_SERVICE_KEY (not used in this deployment)
# ✅ SUPABASE_ANON_KEY is safe (protected by Row Level Security)

# ========================================
# 🧪 TESTING YOUR SETUP
# ========================================
# After setting environment variables in Vercel:
# 1. Trigger a new deployment
# 2. Check /api/r2/health endpoint (if using R2)
# 3. Test image uploads in admin panel
# 4. Verify all functionality works as expected
